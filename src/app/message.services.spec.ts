//import {LoginComponent} from './login.component';
import {MessageService} from "./message.service";

describe('MessageService', () => {

  it ('Añadir mensaje' ,()=>{
  });

  it ('Eliminar matriz cuando esté vacía' ,()=>{
  });

  it ('Eliminar matriz aunque no esté vacía' ,()=>{
  });
});
